// =============================================================================
// Agent Desk — Dashboard (placeholder — dashboard injectors are separate)
// =============================================================================
// Dashboard injection, toolbar building, and agent-terminal mapping are handled
// by the files in dashboard-injectors/. This module is reserved for any
// additional dashboard bridge relay logic.
// =============================================================================

'use strict';

import { registry } from './state.js';

// The dashboard webview loading/error states are handled in views.js
// (setupWebviewStates). Dashboard injectors are loaded separately.

// Reserved for future dashboard bridge relay logic.
